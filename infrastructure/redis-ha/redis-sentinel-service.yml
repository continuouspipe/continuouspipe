apiVersion: v1
kind: Service
metadata:
  labels:
    component-identifier: sentinel
    role: service
  name: redis-sentinel
spec:
  ports:
    - port: 26379
      targetPort: 26379
  selector:
    redis-sentinel: "true"
