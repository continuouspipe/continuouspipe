ui:
  build: .
  volumes:
    - ./app:/app/app
  environment:
    - RIVER_API_URL=river_api.docker
    - AUTHENTICATOR_API_URL=authenticator_app.docker
    - LOG_STREAM_URL=logstream.docker
    - PIPE_API_URL=pipe_api.docker
  labels:
    com.continuouspipe.image-name: inviqasession/cp-river-ui
    com.continuouspipe.visibility: public
  expose:
    - 80
    - 35729
