app:
    image: sroze/php-example
    links:
        - mysql
    environment:
        - MYSQL_PASSWORD=root
mysql:
    image: mysql
    environment:
        MYSQL_PASSWORD: root
    labels:
        com.continuouspipe.update: lock
