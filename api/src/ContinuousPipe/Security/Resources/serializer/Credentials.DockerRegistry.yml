ContinuousPipe\Security\Credentials\DockerRegistry:
    properties:
        id:
            type: integer
            groups: ['Default', 'Credentials']
        username:
            type: string
            groups: ['Default', 'Credentials']
        password:
            type: string
            groups: ['Default', 'Credentials']
        email:
            type: string
            groups: ['Default', 'Credentials']
        serverAddress:
            type: string
            serialized_name: serverAddress
            groups: ['Default', 'Credentials']
            access_type: public_method
        fullAddress:
            type: string
            groups: ['Default', 'Credentials']
        attributes:
            type: array
            groups: ['Default', 'Credentials']
