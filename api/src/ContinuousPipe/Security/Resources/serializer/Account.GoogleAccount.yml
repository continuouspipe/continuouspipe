ContinuousPipe\Security\Account\GoogleAccount:
    properties:
        refreshToken:
            type: string
            groups: ['Default', 'Account']
        serviceAccount:
            type: string
            groups: ['Default', 'Account']
