ContinuousPipe\Security\Credentials\Bucket:
    properties:
        uuid:
            type: string
            groups: ['Default', 'Credentials']
        dockerRegistries:
            type: ArrayCollection<ContinuousPipe\Security\Credentials\DockerRegistry>
            groups: ['Default', 'Credentials']
        gitHubTokens:
            type: ArrayCollection<ContinuousPipe\Security\Credentials\GitHubToken>
            groups: ['Default', 'Credentials']
        clusters:
            type: ArrayCollection<ContinuousPipe\Security\Credentials\Cluster>
            groups: ['Default', 'Credentials']
