ContinuousPipe\Security\Credentials\Cluster\ClusterPolicy:
    properties:
        name:
            type: string
            groups: ['All']
        configuration:
            type: array
            groups: ['Default', 'Credentials']
        secrets:
            type: array<string, string>
            groups: ['Default', 'Credentials']
