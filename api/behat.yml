default:
    suites:
        application:
            contexts:
                - TideContext:
                    - @command_bus
                    - @event_bus
                    - @river.event_store
                    - @river.file_system_resolver.fake
                - LoggingContext:
                    - @log_stream.event_store.in_memory
    extensions:
        Behat\Symfony2Extension:
            kernel:
                env: test

    gherkin:
        filters:
            tags: "~@wip"
