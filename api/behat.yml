default:
    suites:
        application:
            contexts:
                - TideContext:
                    - @command_bus
                    - @river.event_store
                    - @river.file_system_resolver.fake
    extensions:
        Behat\Symfony2Extension:
            kernel:
                env: test

    gherkin:
        filters:
            tags: "~@wip"
